{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions. The sidecar is spawned directly from Rust, so no shell capability is required.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
